// Imported by headless.slint to exercise compileFile() include-path resolution
// (the import must resolve relative to the mounted resource directory).
export component Badge inherits Rectangle {
    in-out property <string> text: "badge";
}
